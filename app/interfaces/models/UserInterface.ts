declare type UserSchema = {
    first_name: string | null,
    last_name: string | null,
    mobile: string,
    email: string | null,
    password: string,
    national_code: string | null,
    created_at: Date,
    updated_at: Date
}
